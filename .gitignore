# Source-of-truth planning notes. Never published.
ignore/

# Cursor workspace config (rules, etc.). Kept local, not published.
.cursor/

# OS and editor cruft
.DS_Store
.idea/
.vscode/
*.swp

# Ebook build output and stray source PDFs (papers are fetched to scratch, never committed)
book/build/
*.epub
*.pdf

# Internal build/publish runbooks. Kept local, not published to the public repo.
book/README.md
book/PUBLISH.md

# Web (Honkit) staged source and rendered output, produced by book/web-render.py
book/web-src/

# Local marketing/design assets, versioned in the private workshop repo
book-prep/
